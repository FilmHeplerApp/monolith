# Соглашения и правила разработки проекта

Этот файл фиксирует правила оформления кода, Git flow и другие аспекты разработки в монолите FilmHelperApp.

Документ пополняется по мере появления договорённостей: каждое новое правило добавляется
отдельным пронумерованным разделом.

---

## 1. Двойной отступ между группами членов класса

### Правило

Внутри тела класса члены группируются, и группы отделяются друг от друга **двумя пустыми
строками**. Внутри одной группы — **одна пустая строка**.

Группа — это набор членов класса, у которых совпадают:

1. вид члена класса (константа / свойство / метод);
2. спецификатор доступа (`public` / `protected` / `private`).

Отсюда два следствия:

- **Между разными спецификаторами доступа** одного и того же вида члена
  (`public` -> `protected` -> `private` константы, свойства, методы) — две пустые строки.
- **Между разными видами членов класса** (константы -> свойства -> методы) — две пустые строки.

Порядок объявления: сначала константы, затем свойства, затем методы. Внутри каждого вида —
от более открытого к более закрытому: `public`, `protected`, `private`.

### Зачем

Границы секций видны сразу, без чтения модификаторов построчно. При беглом просмотре класса
понятно, где заканчивается его публичный контракт и начинается внутренняя кухня.

### Правильно

```php
final class TitleImporter
{
    public const int DEFAULT_LIMIT = 50;
    public const string CACHE_PREFIX = 'import:title:';


    private const int MAX_ATTEMPTS = 3;


    public readonly ProviderSource $source;


    private array $buffer = [];
    private ?ProviderException $failure = null;


    public function __construct(private readonly ProviderClientInterface $client)
    {
        $this->source = $client->source();
    }

    public function import(int $limit = self::DEFAULT_LIMIT): int
    {
        // ...
    }


    protected function shouldRetry(ProviderException $exception): bool
    {
        // ...
    }


    private function flush(): void
    {
        // ...
    }
}
```

### Частные случаи

- Первый член класса пишется сразу после `{`, пустая строка перед ним не ставится.
  Перед закрывающей `}` пустой строки тоже нет.
- Если группа состоит из одного члена — правило не меняется: две пустые строки всё равно
  отделяют её от соседней группы.
- Если какого-то вида членов в классе нет (например, нет констант), лишние пустые строки
  не появляются: правило работает только между реально существующими группами.
- Продвинутые (promoted) свойства конструктора отдельной группой не считаются — они часть
  сигнатуры конструктора, то есть часть группы методов.
- PHPDoc-блок и атрибуты принадлежат своему члену класса и пишутся вплотную к нему.
  Пустые строки ставятся **перед** блоком документации, а не между ним и сигнатурой:

  ```php
  public function failWith(ProviderException $exception, int $afterItems = 0): void
  {
      // ...
  }


  /** @return list<ProviderTitle> */
  private static function defaultFixtures(): array
  {
      // ...
  }
  ```

- `static` сам по себе новую группу не создаёт: группировка идёт по виду члена и
  спецификатору доступа. `public static` и `public` методы лежат в одной группе.
- Правило распространяется на классы, абстрактные классы, трейты и enum'ы. В интерфейсах,
  где все члены публичны, группы обычно две — константы и методы, и они разделяются двумя
  пустыми строками.

---

## 2. Выравнивание параметров в многострочном списке

### Правило

Если список параметров разбит на несколько строк, имена переменных выравниваются в одну
колонку. Строка параметра читается как три колонки:

```text
<спецификатор доступа>  <тип>  <$имя>
```

Ширина колонки типа считается по самому длинному типу в списке и добивается пробелами,
чтобы все `$имена` начинались с одной позиции. Недопустимо писать «спецификатор, тип и
сразу переменную» — тогда имена разъезжаются по длине соседних типов.

Эталон — конструктор [ProviderTitle](../app/Application/Import/DTOs/ProviderTitle.php).

### Зачем

Многострочный список параметров — это, по сути, описание набора полей. В виде таблицы он
читается за один взгляд: слева видно состав, справа — типы. Nullable-типы (`?string`)
перестают теряться в тексте, а при добавлении поля сразу видно, что колонка «поехала» и
блок нужно переформатировать.

### Правильно

```php
final readonly class ProviderTitle
{
    public function __construct(
        public ProviderSource $source,
        public string         $externalId,
        public ?string        $titleRu,
        public ?string        $titleEn,
        public ?string        $description,
        public array          $genres,
        public ?int           $year,
        public ?int           $durationMinutes,
        public ?float         $rating,
        public ?string        $posterUrl,
        public ?string        $bannerUrl,
        public string         $type,
        public string         $status,
    ) {
        // ...
    }
}
```

### Частные случаи

- Каждый параметр многострочного списка пишется на своей строке, в конце — замыкающая
  запятая.
- Однострочная сигнатура не выравнивается: там один пробел между типом и именем —
  `public function import(int $limit = self::DEFAULT_LIMIT): int`.
- Правило работает и для обычных (не promoted) параметров: тогда колонка спецификаторов
  просто пуста, а выравниваются `$имена` относительно колонки типов.
- Значения по умолчанию не выравниваются: `=` пишется сразу после имени через один пробел.
- Атрибуты параметра (`#[...]`) выносятся на отдельную строку над параметром и в расчёте
  колонок не участвуют.
- Аргументы при **вызове** (в том числе именованные) не выравниваются — правило касается
  только объявления параметров:

  ```php
  new ProviderTitle(
      source: ProviderSource::Mock,
      externalId: '5114',
      titleRu: 'Стальной алхимик: Братство',
  );
  ```

- Добавление параметра с более длинным типом требует переформатирования всего блока.
  Это ожидаемая цена правила: такой коммит нормален, отдельно его выделять не нужно.
- Блоки объявления свойств и констант класса (см. правило 1) выравнивать не требуется.

---

## 3. Git flow

### Цикл работы над задачей

```text
Задача в Notion
   -> ветка от актуального main            (3.1)
   -> коммиты на английском                (3.2)
   -> pull request с названием и ссылкой   (3.3, 3.4)
   -> правки по замечаниям + резолв ревьюером (3.5)
   -> merge в main ревьюером               (3.6)
```

Ключевая идея: автор доводит задачу до состояния «готово к проверке», но не закрывает
замечания и не вливает ветку сам. Финальные действия — резолв тредов и merge — выполняет
проверяющий.

### 3.1. Название ветки

Название ветки — **первые 7 символов хеша задачи в Notion**. Хеш берётся из URL задачи:
это 32-символьный идентификатор страницы в конце ссылки.

```text
https://www.notion.so/Abstract-provider-clients-2c4f1a9b3e7d4f6ca1b2c3d4e5f60718
                                                 ^^^^^^^
                                                 2c4f1a9
```

```bash
git switch main && git pull
git switch -c 2c4f1a9
```

**Зачем.** Имя ветки короткое, уникальное и однозначно связано с задачей. Оно не ломается,
если задачу в Notion переименуют, и не требует придумывать транслитерацию названия.

**Частные случаи.**

- Хеш пишется в нижнем регистре, как в URL.
- Одна задача — одна ветка. Если работа разбивается на части, это отдельные задачи в Notion
  со своими хешами.
- Ветка создаётся от актуального `main`, а не от соседней рабочей ветки.
- Правило действует для новых веток. Существующие ветки, названные по смыслу
  (`abstract_provider_clients`, `bug_debug`), доживают как есть и не переименовываются.

### 3.2. Язык commit message

Сообщения коммитов пишутся **только на английском языке**. Это относится и к заголовку, и к
телу коммита.

**Правильно.**

```text
Add mock provider client and provider contracts
Resolve comments in pull request
```

**Неправильно.**

```text
Добавил мок-клиент провайдера
Dobavil mock-klient provaydera
```

**Зачем.** История репозитория остаётся однородной и читаемой в любом инструменте — в `git
log`, в GitHub, в blame внутри IDE. Смешение языков делает историю невозможной для
быстрого просмотра, а транслитерация нечитаема для всех сразу.

**Частные случаи.**

- Название задачи из Notion в коммит переводится по смыслу, а не транслитерируется.
- Названия сущностей кода (классы, методы, таблицы) пишутся как в коде, без перевода.

### 3.3. Название pull request

Название pull request строится по шаблону:

```text
<название ветки>.<название задачи>
```

Название задачи берётся из Notion как есть.

```text
2c4f1a9.Абстракция клиентов провайдеров
```

**Зачем.** В списке PR-ов слева стоит идентификатор, по которому находится и ветка, и
задача, а справа — человекочитаемый смысл. Не нужно открывать PR, чтобы понять, что внутри.

### 3.4. Описание pull request

В описании pull request **обязательна ссылка на задачу в Notion**.

```markdown
Задача: https://www.notion.so/Abstract-provider-clients-2c4f1a9b3e7d4f6ca1b2c3d4e5f60718
```

**Зачем.** Проверяющий должен попадать в контекст задачи одним кликом: требования, критерии
приёмки, обсуждение. Без ссылки ревью превращается в угадывание исходной постановки.

**Частные случаи.**

- Ссылка ставится в описание, а не только в комментарий к PR — комментарии уезжают вниз по
  мере обсуждения.
- Остальное содержимое описания не регламентируется, но ссылка на задачу должна быть видна
  сразу, без раскрытия спойлеров и длинных списков.

### 3.5. Резолв комментариев в pull request

Комментарии и замечания в pull request резолвит **проверяющий**, и только **после нового
коммита** с правками.

Порядок:

1. Проверяющий оставляет замечание.
2. Автор вносит правки и пушит новый коммит в ту же ветку.
3. Проверяющий смотрит изменения и резолвит тред, если замечание закрыто.

**Зачем.** Резолв — это подтверждение проверяющего, что замечание действительно снято, а не
заявление автора о том, что он всё учёл. Если тред закрывает автор, ревьюер теряет список
того, что ещё нужно перепроверить.

**Частные случаи.**

- Автор не резолвит свои треды, даже если правка очевидна. Вместо этого он отвечает в треде.
- Правки по замечаниям оформляются отдельным коммитом — в истории такой коммит выглядит как
  `Resolve comments in pull request`.
- Правки не вливаются в предыдущий коммит через `--amend` с force-push: ревьюеру нужно
  видеть дельту после своего замечания.

### 3.6. Merge в main

После проверки pull request **в `main` вливает проверяющий**, а не автор.

**Зачем.** Состояние `main` — зона ответственности того, кто принял изменения. Пока PR не
влит, у проверяющего остаётся возможность вернуть задачу на доработку; после merge правки
идут уже отдельной задачей.

**Частные случаи.**

- Merge выполняется, когда все треды зарезолвлены (см. 3.5).
- Автор не вливает свой PR сам, даже при тривиальных изменениях.
